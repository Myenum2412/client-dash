# Copy this file to `.env.local` and fill in real values.
#
# Supabase configuration
# - These are safe to expose to the browser (they are "public" by design)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
#
# Optional (server-only). DO NOT expose this to the browser.
# Only needed for admin/server jobs that must bypass RLS.
# SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
#
# Razorpay Payment Gateway Configuration (server-only)
# - Get these from your Razorpay Dashboard: https://dashboard.razorpay.com/
# - DO NOT expose these to the browser
RAZORPAY_KEY_ID=YOUR_RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET=YOUR_RAZORPAY_KEY_SECRET
#
# Razorpay Public Key (client-side, safe to expose)
# - This is used for initializing Razorpay checkout on the frontend
NEXT_PUBLIC_RAZORPAY_KEY_ID=YOUR_RAZORPAY_KEY_ID
